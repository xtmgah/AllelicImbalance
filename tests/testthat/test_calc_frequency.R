

#create test array
#dim 1 - SNPS
#dim 2 - samples
#dim 3 - variants
#ar1 <- array(sample(1:30,size=c(3*2*4)),dim=c(3,2,4))

#vector containing the total number of counts for each sample
#mat <- 1/apply(ar, c(1,2), sum)
#ar2 <- array(as.vector(mat),dim(c(3,2,4)))
#ar3 <- ar1*ar2
#

